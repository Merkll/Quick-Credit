const templateHtml = `
    <div class="menu">
        <span class="logo">Quick Credit</span>
        <nav>
            {{links}}
        </nav>
    </div>
`;


const topMenuTemplate = {
    root: 'top-menu-root',
    classNames: [],
    template: templateHtml
};