/* eslint-disable no-param-reassign */
((global) => {
  const templateHtml = `
   
`;


  const footer = {
    root: 'footer-root',
    classNames: [],
    template: templateHtml
  };
  global.templates.footer = footer;
})(this);
